# Test Suites

Place benchmark-provided regression tests under `tests/benchmark/` and
LLM-generated augmentations under `tests/llm/`. Keep each problem's tests in
its own module (e.g., `tests/benchmark/test_problem_10.py`).
